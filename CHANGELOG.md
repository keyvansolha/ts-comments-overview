# تغییرات / Changelog

## 0.4.0

### افزوده
- **حاشیه‌ی رنگین‌کمانی متحرک روی ورودی «نظرات»**: وقتی برای محصول خلاصه‌ای
  نمایش داده می‌شود، ورودی نظرات در نوار بخش‌های صفحه محصول یک حلقه‌ی
  رنگین‌کمانی متحرک می‌گیرد تا کاربر بفهمد پشت آن تب چیز تازه‌ای هست. حلقه روی
  تب «نظرات» در نوار دسکتاپ و روی میان‌بر نظرات در صفحه موبایل می‌نشیند و اگر
  محصول خلاصه‌ای نداشته باشد، هیچ تغییری در صفحه دیده نمی‌شود.
- **آستانه‌ی کیفیت (min_recommend)**: اگر درصد «پیشنهاد می‌کنند» محصول از این
  عدد کمتر باشد، بخش خلاصه در آن صفحه نمایش داده نمی‌شود و بخش نظرات مثل قبل
  می‌ماند؛ تا جمع‌بندی منفی روی محصولی که کاربران از آن راضی نیستند نمایش داده
  نشود. پیش‌فرض ۵۰ درصد و از «تنظیمات ← خلاصه نظرات محصولات» قابل تغییر است؛
  عدد `0` این قاعده را خاموش می‌کند.
- گزارش آستانه در ابزار «بررسی زنده‌ی سرویس»: برای هر محصول نشان می‌دهد درصد
  پیشنهاد چند است و آیا با تنظیمات فعلی خلاصه نمایش داده می‌شود یا نه.

### تغییر یافته
- نسخه‌ی ساختار تنظیمات به ۲ رسید (کلید جدید `min_recommend`). ذخیره‌ی فقط
  حالت نمایش، مقدار آستانه را پاک نمی‌کند.
- مبنا اول `recommendPercentage` سرویس است و اگر سرویس آن را نداده باشد، سهم
  نظرهای مثبت از مجموع احساسات.

### تست
- ۳۳۶ ادعا در ۶ فایل تست PHP (۳۹ ادعای جدید برای آستانه و نشان‌گذاری نوار).
- تست نگهبان قالب: ناحیه‌ی حلقه‌ی رنگین‌کمانی نشانه‌گذاری شده تا قاعده‌ی
  «فقط توکن‌های طراحی» برای بقیه‌ی فایل استایل معتبر بماند، و وجود ورودی‌های
  نظرات در نوار دسکتاپ/موبایل بررسی می‌شود.

## 0.3.0

### تغییر یافته
- **امتیاز کل و درصد پیشنهاد به هدر منتقل شدند** و به‌صورت دو کارت برجسته کنار عنوان
  نمایش داده می‌شوند (عدد درشت‌تر، آیکون در دایره‌ی رنگ تأکید، سایه‌ی ملایم). این دو
  عدد مهم‌ترین نتیجه‌ی تحلیل‌اند و در ردیف آمار پایین گم می‌شدند.
- تعداد نظرات («۲۰ نظر از سایت‌های دیگر») از کارت آمار به یک خط کوچک زیر زیرعنوان
  منتقل شد؛ ردیف آمار سه‌تایی حذف شد.
- نشان کهنگی داده («به‌روزرسانی در انتظار است») **حذف شد** — همان کارت زردی که در
  سایت دیده می‌شد و برای بازدیدکننده معنای روشنی نداشت.

## 0.2.0

### اصلاح‌شده
- **رفع خطای `ts_comments_overview_empty` روی پاسخ واقعی سرویس.** پاسخ سرویس تحلیل
  را داخل کلید `analysis` برمی‌گرداند (`{ woocommerce_id, analysis: {…} }`) و
  افزونه این پوشش را نمی‌شناخت؛ در نتیجه هر محصول «بدون داده» تلقی می‌شد. حالا
  `analysis` و هر پوشش دیگری با همان شکل رکورد تشخیص داده می‌شود.
- **خواندن ساختار واقعی `topics` و `strengths`/`weaknesses`.** این فیلدها آبجکت‌اند
  (`{label, count}` و `{text, count}`) نه رشته‌ی ساده؛ شمارش هر آیتم هم نمایش داده
  می‌شود.
- **مبنای نوار احساسات.** سرویس `sentiment` را می‌تواند درصدی بفرستد (۸۰/۱۰/۱۰) در
  حالی که `totalComments` عدد واقعی نظرات است؛ برچسب «بر پایه‌ی N نظر» از
  `totalComments` می‌آید تا عدد نادرست چاپ نشود.

### تغییر یافته
- **زمان آخرین به‌روزرسانی دیگر در رابط کاربری نمایش داده نمی‌شود** (داده‌ی
  `updatedAt` در ساختار داخلی و ابزار پیشخوان باقی می‌ماند).
- **نام سایت‌های مرجع و تعدادشان حذف شد**؛ نه فهرست منابع و نه نشان «بر پایه‌ی N منبع».
- **عنوان بخش صریح شد:** «آنچه کاربران در اینترنت درباره این محصول می‌گویند».
- **هشدار برجسته پیش از متن خلاصه:** کاربر باید پیش از خواندن خلاصه بداند این نظرات
  از سایت‌های دیگر است و ایرادهایی مثل بسته‌بندی نامناسب یا کالای آسیب‌دیده مربوط
  به همان فروشگاه‌هاست، نه خرید از تهران‌اسپیکر.
- `direction` موضوع‌ها فقط در ساختار داخلی نگه داشته می‌شود و در UI نمایش داده
  نمی‌شود (معنای دقیقش از سرویس تأیید نشده است).

### افزوده
- **نمونه‌ی پاسخ واقعی سرویس** در `tests/fixtures/` و تست سرتاسری روی همان پاسخ.
- حالت اختیاری تست زنده (`TSCO_REAL_HTTP=1` + `TSCO_API_TOKEN`) که کد واقعی کلاینت
  را به سرویس واقعی می‌زند.
- تست‌های نگهبان سمت قالب برای توکن‌های طراحی، هوک، escape، نبود منبع و نبود تاریخ.

## 0.1.0 — نسخه‌ی اولیه
- بخش «خلاصه نظرات کاربران» در بخش نظرات صفحه محصول (دسکتاپ و موبایل) با سه حالت
  نمایش (خاموش / خلاصه / خلاصه + نقاط قوت و ضعف).
- توکن سرویس از `wp-config.php` (هرگز در دیتابیس)، کش ترنزینت موفق و ناموفق،
  صفحه‌ی تنظیمات با ابزار بررسی زنده و پاک‌سازی کش.
- استایل کاملاً بر پایه‌ی توکن‌های معنایی قالب Amazing (دارک/لایت خودکار).
